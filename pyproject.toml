[project]
name = "atp-examples"
version = "0.2.0"
description = "Runnable examples for the Agent Trust Protocol Python SDK"
requires-python = ">=3.12"
dependencies = [
    "atp-python",
    "fastapi>=0.128.0",
    "uvicorn>=0.40.0",
    "httpx>=0.28.1",
    "python-dotenv>=1.2.2",
]

[tool.uv.sources]
atp-python = { git = "https://github.com/aiquilibria/atp-python.git" }
